# ZenMoney Personal Finance Assistant

Управляй своими финансами через ZenMoney прямо в Claude Code. Анализируй расходы, создавай транзакции, планируй бюджет — всё через естественный диалог.

## Capabilities

- 📊 **Финансовая аналитика**: Анализ расходов и доходов по категориям, счетам, периодам
- 💳 **Управление транзакциями**: Создание, изменение, удаление транзакций
- 🏦 **Счета**: Просмотр балансов, создание новых счетов
- 📈 **Бюджетирование**: Проверка бюджетов, планирование расходов
- 🎯 **Умные подсказки**: ML-рекомендации категорий для транзакций
- 📅 **Напоминания**: Просмотр запланированных платежей

## Quick Start

### Первый запуск

Если MCP сервер ещё не настроен:

```bash
# 1. Собрать проект
npm install
npm run build

# 2. Получить токен (выбери один из способов):

# Способ A: Из BUDGERA (самый простой для существующих пользователей)
# Открой https://budgera.com/settings/export
# Скопируй токен из настроек
# Вставь в .mcp.json в поле ZENMONEY_TOKEN

# Способ B: Через браузерную авторизацию
npm run auth

# 3. Перезапустить Claude Code
```

### Основные команды

**Анализ финансов:**
- "Сколько я потратил в этом месяце?"
- "Покажи расходы по категориям за январь"
- "Проанализируй мои траты на рестораны"
- "Смогу ли я отвести девушку на свидание?"

**Управление транзакциями:**
- "Добавь расход 500 рублей на кофе"
- "Создай перевод 10000 с карты на наличные"
- "Удали последнюю транзакцию"
- "Измени категорию у транзакции на Продукты"

**Просмотр данных:**
- "Покажи все мои счета"
- "Какой баланс на карте?"
- "Список всех категорий расходов"
- "Запланированные платежи на этот месяц"

## Available Tools

### Чтение данных

#### `get_accounts`
Получить все счета с балансами.

**Параметры:**
- `include_archived` (boolean) - Включить архивные счета

**Пример:**
```
Покажи все мои счета включая архивные
```

#### `get_transactions`
Получить транзакции за период.

**Параметры:**
- `start_date` (string, обязательный) - Дата начала (yyyy-MM-dd)
- `end_date` (string) - Дата окончания (по умолчанию: сегодня)
- `account_id` (string) - Фильтр по счёту
- `category_id` (string) - Фильтр по категории
- `limit` (number) - Максимум результатов (по умолчанию: 100, макс: 500)

**Пример:**
```
Покажи все расходы за последние 30 дней
Транзакции по карте Тинькофф за январь
```

#### `get_categories`
Получить все категории (теги) в иерархической структуре.

**Пример:**
```
Покажи все категории расходов
Какие есть подкатегории в Продуктах?
```

#### `get_instruments`
Получить список валют и курсы обмена.

**Пример:**
```
Какие валюты поддерживаются?
Покажи курс доллара
```

#### `get_budgets`
Просмотр бюджетов на месяц.

**Параметры:**
- `month` (string, обязательный) - Месяц в формате yyyy-MM

**Пример:**
```
Покажи бюджет на январь 2026
Какой бюджет на текущий месяц?
```

#### `get_reminders`
Запланированные платежи и напоминания.

**Параметры:**
- `include_processed` (boolean) - Включить обработанные напоминания

**Пример:**
```
Какие платежи запланированы?
Покажи все напоминания
```

#### `get_analytics`
Аналитика расходов и доходов с группировкой.

**Параметры:**
- `start_date` (string, обязательный) - Дата начала
- `end_date` (string) - Дата окончания (по умолчанию: сегодня)
- `group_by` (string) - Группировка: 'category', 'account', 'merchant' (по умолчанию: 'category')
- `type` (string) - Тип: 'expense', 'income', 'all' (по умолчанию: 'expense')

**Пример:**
```
Анализ расходов по категориям за февраль
Сколько потратил в каждом магазине за месяц?
Доходы по счетам за год
```

#### `suggest`
ML-подсказки категорий для названия транзакции.

**Параметры:**
- `payee` (string, обязательный) - Название продавца/получателя

**Пример:**
```
Какую категорию предложить для "Пятёрочка"?
Подскажи категорию для транзакции в Стarbucks
```

### Запись данных

#### `create_transaction`
Создать новую транзакцию.

**Параметры:**
- `type` (string, обязательный) - Тип: 'expense', 'income', 'transfer'
- `amount` (number, обязательный) - Сумма (положительное число)
- `account_id` (string, обязательный) - ID счёта (источник для расхода/перевода, получатель для дохода)
- `to_account_id` (string) - ID счёта назначения (только для переводов)
- `category_ids` (array) - ID категорий
- `date` (string) - Дата в формате yyyy-MM-dd (по умолчанию: сегодня)
- `payee` (string) - Название продавца/получателя
- `comment` (string) - Комментарий

**Пример:**
```
Добавь расход 1500 рублей на кофе
Создай доход 50000 как зарплату на карту
Перевод 10000 с карты на наличные
```

#### `update_transaction`
Изменить существующую транзакцию.

**Параметры:**
- `id` (string, обязательный) - UUID транзакции
- `amount` (number) - Новая сумма
- `category_ids` (array) - Новые категории
- `payee` (string) - Новое название продавца
- `comment` (string) - Новый комментарий

**Пример:**
```
Измени сумму транзакции на 2000
Добавь комментарий "деловой обед" к последней транзакции
```

#### `delete_transaction`
Удалить транзакцию.

**Параметры:**
- `id` (string, обязательный) - UUID транзакции

**Пример:**
```
Удали последнюю транзакцию
Удали ошибочный платёж
```

#### `create_account`
Создать новый счёт.

**Параметры:**
- `title` (string, обязательный) - Название счёта
- `type` (string, обязательный) - Тип: 'cash', 'ccard', 'checking'
- `currency_id` (number, обязательный) - ID валюты из get_instruments
- `balance` (number) - Начальный баланс (по умолчанию: 0)
- `credit_limit` (number) - Кредитный лимит для карт (по умолчанию: 0)

**Пример:**
```
Создай кошелёк "Наличные доллары" в долларах
Добавь счёт "Альфа-банк" типа checking
```

### Системные

#### `check_auth_status`
Проверить статус авторизации и валидность токена.

**Пример:**
```
Проверь подключение к ZenMoney
Рабочий ли токен?
```

## Usage Examples

### Пример 1: Месячный анализ расходов

```
Пользователь: Сколько я потратил в феврале 2026?

Claude:
1. Использует get_analytics с параметрами:
   - start_date: "2026-02-01"
   - end_date: "2026-02-28"
   - type: "expense"
   - group_by: "category"

2. Показывает:
   - Общую сумму расходов
   - Топ-5 категорий по тратам
   - Процентное распределение
   - Сравнение с предыдущим месяцем (если запросить)
```

### Пример 2: Планирование свидания

```
Пользователь: Смогу ли я отвести девушку на свидание в этом месяце?

Claude:
1. get_budgets для текущего месяца → проверить бюджет на "Рестораны/Развлечения"
2. get_analytics за текущий месяц → сколько уже потрачено
3. get_accounts → текущие балансы счетов
4. Анализирует:
   - Остаток бюджета на развлечения
   - Доступные средства на счетах
   - Запланированные обязательные платежи
5. Даёт рекомендацию: "Да, можешь потратить ~X рублей" или "Лучше подождать до зарплаты"
```

### Пример 3: Быстрое добавление расхода

```
Пользователь: Купил кофе за 250 рублей

Claude:
1. suggest с payee: "кофе" → получить рекомендуемую категорию
2. get_accounts → найти основной счёт для списания
3. create_transaction:
   - type: "expense"
   - amount: 250
   - payee: "Кофе"
   - category_ids: [рекомендованная категория]
   - account_id: [основной счёт]
4. Подтверждает: "✅ Добавлен расход 250₽ на кофе в категорию 'Кафе и рестораны'"
```

### Пример 4: Анализ по периоду расчёта

```
Пользователь: Анализ за мой расчётный период (20 января - 19 февраля)

Claude:
1. get_analytics:
   - start_date: "2026-01-20"
   - end_date: "2026-02-19"
   - type: "all"
2. get_budgets для месяца
3. Показывает:
   - Доходы: XX₽
   - Расходы: YY₽
   - Профицит/дефицит: ZZ₽
   - Топ категорий расходов
   - Соблюдение бюджета
```

## Smart Features

### Автоматическое определение контекста

Claude автоматически понимает твои запросы без точной формулировки:

- "Сколько потратил?" → анализ расходов за текущий месяц
- "Добавь кофе 200р" → создание расхода с ML-категорией
- "Баланс" → показать все счета с балансами
- "Свидание" → анализ бюджета на развлечения

### Умные категории

При создании транзакций Claude:
1. Использует `suggest` для ML-подсказки категории
2. Предлагает наиболее вероятную категорию
3. Спрашивает подтверждение если неуверен

### Естественные даты

Поддерживает понятные формулировки:
- "в этом месяце" → текущий календарный месяц
- "за последние 30 дней" → от сегодня - 30 дней
- "в январе" → 2026-01-01 до 2026-01-31
- "в прошлом году" → весь предыдущий год

### Мультивалютность

Автоматически:
- Показывает балансы в родной валюте счёта
- Использует курсы из `get_instruments`
- Конвертирует при необходимости

## Troubleshooting

### Ошибка "No authentication configured"

**Решение (выбери один из способов):**

**Способ 1: BUDGERA (самый быстрый)**
1. Открой https://budgera.com/settings/export
2. Скопируй токен из раздела "ZenMoney API Token"
3. Вставь в `.mcp.json`:
   ```json
   {
     "mcpServers": {
       "zenmoney": {
         "env": {
           "ZENMONEY_TOKEN": "скопированный_токен"
         }
       }
     }
   }
   ```
4. Перезапусти Claude Code

**Способ 2: Браузерная авторизация**
```bash
npm run auth
# После успешной авторизации перезапустить Claude Code
```

### Ошибка 401 / Token expired

**Решение (выбери один из способов):**

**Способ 1: BUDGERA**
1. Открой https://budgera.com/settings/export
2. Скопируй новый токен
3. Обнови в `.mcp.json`
4. Перезапусти Claude Code

**Способ 2: npm run auth**
```bash
npm run auth
```

**Проверить статус:**
```
Проверь подключение к ZenMoney
```

### Пустые результаты

Возможные причины:
1. Данных действительно нет за указанный период
2. Кэш не синхронизирован (перезапустить Claude Code)
3. Неверный ID счёта/категории

**Решение:**
```
Покажи все счета  # проверить что счета есть
Покажи категории  # проверить структуру категорий
```

## Technical Details

### Архитектура

```
[Claude Code] → [ZenMoney Skill] → [MCP Server] → [ZenMoney API]
                                           ↓
                                      [DataCache]
```

### Кэширование

- Первый запрос: полная синхронизация всех данных
- Последующие: инкрементальная синхронизация (только изменения)
- Кэш хранится в памяти (сбрасывается при перезапуске)

### Безопасность

- Токен хранится в `.mcp.json` (git-ignored)
- Все запросы через HTTPS
- Credentials никогда не логируются
- Token автоматически обновляется при истечении (для credential-based auth)

## Advanced Usage

### Пользовательские периоды расчёта

Если твой расчётный период не календарный месяц:

```
Анализ с 20 января по 19 февраля  # явно указать даты
Расходы за мой период (20-19)      # Claude запомнит паттерн
```

### Групповой анализ

```
Покажи топ-10 магазинов по тратам за год
Сравни расходы по месяцам в 2025 и 2026
Динамика расходов на продукты за последние 6 месяцев
```

### Bulk операции

```
Создай 5 транзакций: [список]
Обнови категории у всех транзакций от "Перекрёсток"
Удали все тестовые транзакции
```

## Integration Tips

### С другими MCP серверами

ZenMoney skill хорошо работает вместе с:
- **Google Sheets**: Экспорт финансовой аналитики в таблицы
- **Browser Automation**: Автоматизация платежей
- **Filesystem**: Сохранение отчётов в файлы

### Custom workflows

Создавай собственные финансовые команды:

```markdown
# Моя команда: ежемесячный отчёт

1. Получить аналитику за текущий месяц
2. Сравнить с бюджетом
3. Экспортировать в Google Sheets
4. Отправить уведомление если перерасход
```

## Best Practices

1. **Регулярно проверяй auth**: `check_auth_status` раз в неделю
2. **Используй категории**: Всегда указывай категорию при создании транзакций
3. **Описывай payee**: ML-модель лучше работает с конкретными названиями
4. **Проверяй перед удалением**: Удаление транзакций необратимо
5. **Sync периодически**: Перезапускай Claude Code если данные устарели

## Roadmap

Планируемые возможности:
- [ ] Автоматическая категоризация транзакций
- [ ] Предиктивная аналитика расходов
- [ ] Голосовой ввод транзакций
- [ ] Интеграция с банковскими выписками
- [ ] Создание и управление бюджетами через MCP
- [ ] Создание напоминаний о платежах

---

**Автор**: ZenMoney MCP Server
**Версия**: 1.0.0
**Лицензия**: MIT
**Документация**: [README.md](./README.md)