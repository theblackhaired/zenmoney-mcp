# ZenMoney Skill Installation Guide

## Установка skill в Claude Code

### Вариант 1: Через симлинк (рекомендуется)

**Windows (PowerShell с правами администратора):**
```powershell
# Создать директорию для skills если её нет
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.claude\skills"

# Создать символическую ссылку на skill файл
New-Item -ItemType SymbolicLink -Path "$env:USERPROFILE\.claude\skills\zenmoney.skill.md" -Target "$PWD\zenmoney.skill.md"
```

**macOS/Linux:**
```bash
# Создать директорию для skills если её нет
mkdir -p ~/.claude/skills

# Создать символическую ссылку на skill файл
ln -s "$(pwd)/zenmoney.skill.md" ~/.claude/skills/zenmoney.skill.md
```

### Вариант 2: Через копирование

**Windows:**
```powershell
Copy-Item "zenmoney.skill.md" -Destination "$env:USERPROFILE\.claude\skills\zenmoney.skill.md"
```

**macOS/Linux:**
```bash
cp zenmoney.skill.md ~/.claude/skills/zenmoney.skill.md
```

### Вариант 3: Указать путь в настройках

Добавить в `.claude/settings.json`:

```json
{
  "skills": {
    "directories": [
      "C:\\Users\\kirill.gorosov\\Documents\\Dev\\zenmoney-skill"
    ]
  }
}
```

## Проверка установки

1. Перезапустить Claude Code
2. В чате написать: `/skills` или "покажи доступные skills"
3. В списке должен появиться `zenmoney`

## Использование

После установки можно использовать skill:

```
/zenmoney Сколько я потратил в этом месяце?
```

Или просто естественным языком:
```
Покажи мои расходы через ZenMoney
Добавь транзакцию в ZenMoney: кофе 250р
```

Claude автоматически распознает запросы к ZenMoney и использует нужный skill.

## Обновление skill

Если skill был установлен через симлинк, изменения в `zenmoney.skill.md` автоматически подхватятся.

Если через копирование, нужно повторить копирование:

```powershell
# Windows
Copy-Item "zenmoney.skill.md" -Destination "$env:USERPROFILE\.claude\skills\zenmoney.skill.md" -Force
```

```bash
# macOS/Linux
cp zenmoney.skill.md ~/.claude/skills/zenmoney.skill.md
```

## Удаление skill

**Windows:**
```powershell
Remove-Item "$env:USERPROFILE\.claude\skills\zenmoney.skill.md"
```

**macOS/Linux:**
```bash
rm ~/.claude/skills/zenmoney.skill.md
```

## Troubleshooting

### Skill не появляется в списке

1. Проверить путь к файлу:
   ```powershell
   Test-Path "$env:USERPROFILE\.claude\skills\zenmoney.skill.md"
   ```

2. Проверить формат файла (должен быть `.skill.md`)

3. Перезапустить Claude Code полностью (не только чат)

### MCP сервер не подключён

Skill использует MCP сервер. Убедись что:

1. MCP сервер собран: `npm run build`
2. Токен настроен одним из способов:
   - **Через BUDGERA (самый простой)**: https://budgera.com/settings/export
   - **Через браузер**: `npm run auth`
3. MCP сервер зарегистрирован в `.mcp.json`:

```json
{
  "mcpServers": {
    "zenmoney": {
      "command": "node",
      "args": ["C:\\Users\\kirill.gorosov\\Documents\\Dev\\zenmoney-skill\\dist\\index.js"],
      "env": {
        "ZENMONEY_TOKEN": "your-token-here"
      }
    }
  }
}
```

4. Перезапустить Claude Code после настройки MCP

### Skill работает, но нет данных

1. Проверить авторизацию:
   ```
   Проверь подключение к ZenMoney
   ```

2. Обновить токен:
   ```bash
   npm run auth
   ```

3. Перезапустить Claude Code

## Расширенная настройка

### Кастомизация skill

Отредактируй `zenmoney.skill.md` под свои нужды:

1. Добавь свои часто используемые команды
2. Настрой примеры под свой расчётный период
3. Добавь кастомные категории
4. Создай шаблоны транзакций

Пример кастомного раздела:

```markdown
## Мои шаблоны

### Ежедневные расходы
- Кофе утром: 250₽ → Кафе
- Обед: 500₽ → Рестораны
- Метро: 100₽ → Транспорт

### Команды
- "утренний кофе" → создать расход 250₽ категория "Кафе"
- "обед" → создать расход 500₽ категория "Рестораны"
```

### Интеграция с другими skills

Можно комбинировать ZenMoney с другими skills:

```markdown
## Workflow: Ежемесячный отчёт

1. ZenMoney: Получить аналитику за месяц
2. Google Sheets: Экспортировать в таблицу
3. Email: Отправить себе отчёт
```

---

**Готово!** 🎉 Теперь ты можешь управлять финансами прямо в Claude Code!